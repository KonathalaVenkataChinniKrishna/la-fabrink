import { LafabrinkPage } from './app.po';

describe('decima App', () => {
  let page: LafabrinkPage;

  beforeEach(() => {
    page = new LafabrinkPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
